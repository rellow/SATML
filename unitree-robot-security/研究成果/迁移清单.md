# 宇树机器人安全研究漏洞迁移清单

生成日期：2026-09-11  
状态：已复核的迁移清单；目标目录原有中文骨架保留不覆盖，本次只允许导入仓库外生成的脱敏副本。

## 路径与规范冲突

- 请求中的 `C:\zyh\work\漏洞整理\unitree\_GO2` 与 `C:\zyh\work\漏洞整理\unitree\_R1` 不存在；实际对应目录为 `C:\zyh\work\漏洞整理\unitree_GO2` 与 `C:\zyh\work\漏洞整理\unitree_R1`。后续以实际目录为只读主来源。
- 请求中的目标父目录在初次盘点时尚未创建；目标仓库随后以非 Git、非空中文骨架形式出现。本次将其作为既有基线增量审计，不覆盖、不删除，并不改用现有 `C:\zyh\work\void_projects`。
- GO2 `26_DDS身份私钥随固件分发` 的旧索引结论与后续公开固件复核冲突：后续复核未发现私钥随固件分发，因此以 `已证伪` 迁移并保留冲突说明。
- GO2 `21_config_set疑似路径写` 的旧候选结论与六种真机路径变体均被拒绝的后续结果冲突，因此以 `已证伪` 迁移。
- GO2 BMS 欺骗话题由旧报告的 `rt/lf/bmsstate` 修正为 `rt/lowstate`；采用后续修正。
- R1 MQTT answer 注入的机器人侧处理逻辑存在，但生产 broker ACL 阻断了攻击者发布路径；按互联网攻击路径 `已证伪` 迁移，代码事实作为防御纵深证据保留。
- R1 `basic_service` 未捕获 JSON 崩溃被 `Thread::Wrap` 异常捕获机制推翻，以 `已证伪` 迁移。

## 稳定编号总览

### GO2

| 编号 | 漏洞简称 | 验证状态 | 主要来源 | 迁移处理 |
|---|---|---|---|---|
| G2-001 | 零凭证未授权控制 | 动态确认 | `Go2_零凭证未授权控制`、未动态闭环 01 | 合并重复报告与 PoC，仅保留一份权威 README |
| G2-002 | 编程执行器到 root RCE | 动态确认 | `Go2_沙箱逃逸root_RCE` | 迁移脱敏复现与最小证据 |
| G2-003 | BLE 配网注入与网络劫持 | 候选 | `Go2_BLE配网注入劫持网络` | 保留待真机闭环限制 |
| G2-004 | OTA 固件远程获取链 | 动态确认 | `Go2_OTA固件远程获取链` | 仅保留脱敏脚本与小型证据，固件包不提交 |
| G2-005 | RTSP 无认证视频流 | 静态确认 | 未动态闭环 02 | 迁移报告与安全探针说明 |
| G2-006 | 前摄明文 RTP 组播 | 静态确认 | 未动态闭环 03 | 迁移报告与证据摘要 |
| G2-007 | 伪造低状态致阻尼瘫倒 | 静态确认 | 未动态闭环 04 | 使用修正后的 `rt/lowstate` 结论 |
| G2-008 | master_service 本地 UDS 无鉴权 RPC | 静态确认 | 未动态闭环 05 | 明确仅本地/后渗透前提 |
| G2-009 | DDS 运动控制无鉴权 | 静态确认 | 未动态闭环 06 | 明确 eth0/内部总线可达性 |
| G2-010 | BLE 未授权 SN 泄露 | 静态确认 | 未动态闭环 07 | 完整 SN 必须脱敏 |
| G2-011 | BLE F2 密钥投递 | 静态确认 | 未动态闭环 08 | 保留每设备密钥来源更正，不提交密钥 |
| G2-012 | 凭据明文日志落盘 | 静态确认 | 未动态闭环 09 | 仅保留完全脱敏的字段级证据 |
| G2-013 | UDP 伪发现以 SN 为唯一凭据 | 动态确认 | 未动态闭环 10、零凭证控制闭环 | 与 G2-001 关联，不复制 PoC |
| G2-014 | BLE 链路层零保护 | 静态确认 | 未动态闭环 11 | 迁移静态证据 |
| G2-015 | BLE 握手零熵与授权态残留 | 动态确认 | 未动态闭环 12、BLE 真机链 | 迁移脱敏会话关联 |
| G2-016 | BLE 改写 RemoteConnectionEnabled | 静态确认 | 未动态闭环 13 | 说明需要有效会话密钥 |
| G2-017 | WiFi 凭据落盘与默认口令 | 静态确认 | 未动态闭环 14 | 不提交任何口令原值 |
| G2-018 | ttyS3 语音芯片帧注入 | 静态确认 | 未动态闭环 15 | 明确本地串口前提 |
| G2-019 | 语音芯片无签名烧录持久化 | 候选 | 未动态闭环 16 | 自动烧录路径未武装，保留人工触发限制 |
| G2-020 | vui/audiohub API 零门槛 | 静态确认 | 未动态闭环 17 | 与 G2-001 关联 |
| G2-021 | log_uploader 轨迹上云 | 静态确认 | 未动态闭环 19 | 不提交用户轨迹原始日志 |
| G2-022 | vui_service 启动 CDN 覆盖写 | 静态确认 | 未动态闭环 20 | 作为供应链风险记录 |
| G2-023 | config_set 路径写 | 已证伪 | 未动态闭环 21、后续真机复核 | 保留六种变体被拒的证伪过程 |
| G2-024 | webrtc_bridge 无界 strcpy | 静态确认 | 未动态闭环 22 | 迁移指令级结论，不导入武器化输入 |
| G2-025 | REQ.ipc 零认证电机 RPC | 静态确认 | 未动态闭环 24 | 明确本地 IPC/后渗透前提 |
| G2-026 | 全服务 root 无降权 | 静态确认 | 未动态闭环 25 | 作为架构放大器而非独立远程入口 |
| G2-027 | DDS 身份私钥随固件分发 | 已证伪 | 未动态闭环 26、后续公开包复核 | 不提交任何私钥样本，保留证伪证据 |
| G2-028 | 串口自动登录运维痕迹 | 候选 | 未动态闭环 30 | 标记待真机物理核验 |

### R1

| 编号 | 漏洞简称 | 验证状态 | 主要来源 | 迁移处理 |
|---|---|---|---|---|
| R1-001 | audio_detect socket-to-shell | 动态确认 | `R1_audio_detect网络rce` | 固定认证值全部脱敏 |
| R1-002 | App WebView 桥劫持 | 动态确认 | `APPbridge劫持` | 合并代码引用与动态日志 |
| R1-003 | arm_service 无鉴权 RPC | 动态确认 | `arm服务rename穿越RCE链` | 作为 R1-CHAIN-001 第一阶段 |
| R1-004 | arm_service 任意文件投放 | 动态确认 | 同上 | 作为 R1-CHAIN-001 第二阶段 |
| R1-005 | arm_service rename 路径穿越 | 动态确认 | 同上 | 作为 R1-CHAIN-001 第三阶段 |
| R1-006 | chat_go 知识库路径穿越写入 | 动态确认 | `chat_go知识库任意文件写RCE` | 作为 R1-CHAIN-002 投放原语 |
| R1-007 | OTA 固件远程获取 | 动态确认 | `OTA固件远程获取链` | 不提交 UPK、TAR 或解包树 |
| R1-008 | TURN 中继滥用 | 动态确认 | `TURN中继滥用` | 不提交 TURN 凭据原值 |
| R1-009 | 固件硬编码云凭据泄露 | 动态确认 | AWS、DashScope/博查及“硬编码凭证全线泄露”三个重复包 | 合并为一个权威报告，不提交有效凭据或验证日志原值 |
| R1-010 | gpt-proxy 以 SN 认证 | 动态确认 | `云端WSS_gpt-proxy_SN即凭证` | 完整 SN 脱敏，保留阴性影响结论 |
| R1-011 | 云端知识库未授权写入 | 动态确认 | `云端知识库未授权写入` | 区分云端写入与 R1 消费链被阻断 |
| R1-012 | 共享 aes_key 体系失效 | 动态确认 | `共享密钥体系失效_aes_key` | 不提交密钥字节，只保留交叉验证摘要 |
| R1-013 | aisport 配置越界堆写 | 静态确认 | `AUD-r3-aisport-config-oob-heapwrite` | 迁移静态分析与非武器化探针 |
| R1-014 | aisport 配置污染组合链 | 候选 | `AUD-r3-aisport-config-poison-composition` | 标明写入内容受限 |
| R1-015 | audio_detect 任意文件读 | 静态确认 | `AUD-audiodetect-02-arbitrary-file-read` | 私钥、shadow 等输出不提交 |
| R1-016 | audio_detect DDS 摇杆武装 | 静态确认 | `AUD-audiodetect-03-dds-joystick-spawn` | 迁移可达性分析 |
| R1-017 | KVS DCEP 标签溢出 | 静态确认 | `AUD-kvsmem-dcep-label-overflow` | 仅保留崩溃级 PoC |
| R1-018 | KVS offer 栈溢出 | 静态确认 | `AUD-kvsmem-offer-stack-overflow` | 仅保留崩溃级 PoC |
| R1-019 | KVS offer reqId 溢出 | 静态确认 | `AUD-kvsmem-offerreqid-overflow` | 仅保留崩溃级 PoC |
| R1-020 | KVS offer token 溢出 | 静态确认 | `AUD-kvsmem-offertoken-overflow` | 仅保留崩溃级 PoC |
| R1-021 | KVS SDP 属性名堆溢出 | 静态确认 | `AUD-kvsmem-sdp-attr-overflow` | 修正上游修复版本与 broker 可达性 |
| R1-022 | RTCP NACK 越界读 | 静态确认 | `AUD-r3-rtcp-nack-oob-read` | 迁移静态证据 |
| R1-023 | RTCP TWCC 越界读 | 静态确认 | `AUD-r3-rtcp-twcc-oob-read` | 迁移静态证据 |
| R1-024 | motion_switcher form Cmd 命令执行 | 静态确认 | `AUD-r3-motionsw-form-cmd-root-rce` | 限定本地或 OTA 写入前提 |
| R1-025 | MQTT WebRTC answer 注入 | 已证伪 | `AUD-p3-mqtt-webrtc-answer-injection` | 保留 broker ACL 阻断证据 |
| R1-026 | corr service Markdown 持久化 | 候选 | `AUD-corr-service-dir-md-persistence` | 加载跳待真机确认 |
| R1-027 | msvc service Cmd 文件 RCE | 静态确认 | `AUD-msvc-service-cmd-file-rce` | 迁移静态分析 |
| R1-028 | msvc 无鉴权 RPC socket | 静态确认 | `AUD-msvc-unauth-rpc-socket` | 明确本地/后渗透前提 |
| R1-029 | OTA TAR 路径穿越 | 静态确认 | `AUD-ota-tar-traversal` | 不提交 TAR/UPK |
| R1-030 | OTA 验签绕过 | 静态确认 | `AUD-ota-verify-bypass` | 不提交固件包 |
| R1-031 | audiohub 无界上传 | 静态确认 | `AUD-audiohub-upload-unbounded` | 仅保留资源上限探针 |
| R1-032 | chat_go LLM API 重定向 | 静态确认 | `AUD-chatgo-llmapi-redirect` | 迁移静态分析 |
| R1-033 | espeak punctlist BSS 溢出 | 候选 | `AUD-espeak-punctlist-bss-overflow` | 运行时引擎待确认 |
| R1-034 | espeak removeEnding 栈溢出 | 候选 | `AUD-espeak-removeending-stack-overflow` | 运行时引擎待确认 |
| R1-035 | vui voice1003 limiter 堆溢出 | 静态确认 | `AUD-vui-voice1003-limiter-heapoverflow` | 保留语义限制 |
| R1-036 | config KV 污染 | 静态确认 | `AUD-config-kv-poisoning` | 迁移静态分析 |
| R1-037 | DDS 全栈默认身份材料 | 静态确认 | `AUD-dds-fullstack-defaults` | 不提交任何身份私钥 |
| R1-038 | demarcate 急停重绑定 | 静态确认 | `AUD-demarcate-estop-rebind` | 明确内部网/桥白名单前提 |
| R1-039 | basic_service 未捕获 JSON 崩溃 | 已证伪 | `AUD-r3-basicsvc-uncaught-json-crash` | 保留异常捕获反证 |
| R1-040 | BMS 状态欺骗阻尼 | 静态确认 | `AUD-r3-batguard-bmsstate-spoof-damp` | 降为 eth0 内部网威胁 |
| R1-041 | autotest 双臂物理劫持 | 静态确认 | `AUD-r6-autotest-arm-physical-hijack` | 迁移安全探针 |
| R1-042 | robot_type 自复制耗尽文件系统 | 静态确认 | `AUD-robottype-selfcopy-fs-exhaustion` | 迁移静态分析 |
| R1-043 | robot_type 路径穿越复制 | 静态确认 | `AUD-robottype-traversal-copy` | 迁移静态分析 |

### 跨型号

| 编号 | 漏洞简称 | 验证状态 | GO2 状态 | R1 状态 | 迁移处理 |
|---|---|---|---|---|---|
| UT-001 | btgatt-server 分片重组溢出到 root RCE | 动态确认 | 动态确认 | 动态确认 | 合并两份报告；GO2/R1 复现与证据分目录，原始 `aes_key.bin` 不提交 |
| UT-002 | AES-GCM 解密先写后验栈溢出 | 动态确认 | 动态确认（DoS） | 静态确认 | 合并共同根因；分别说明型号结论，不夸大 R1 动态状态 |

## 攻击链

| 编号 | 名称 | 状态 | 组成漏洞 |
|---|---|---|---|
| GO2-CHAIN-001 | 零凭证接入到 root RCE | 已动态闭环 | G2-001、G2-002 |
| GO2-CHAIN-002 | BLE 配网到网络劫持 | 待动态闭环 | G2-003、G2-011、G2-015 |
| R1-CHAIN-001 | arm_service 文件投放与 rename 到 root | 已动态闭环 | R1-003、R1-004、R1-005 |
| R1-CHAIN-002 | chat_go 写入到 bashrunner root | 已动态闭环 | R1-006 |
| UT-CHAIN-001 | BLE 分片重组到 root 命令执行 | 已动态闭环（双型号） | UT-001 |

攻击链目录只保存说明与 SVG，不复制漏洞报告、PoC 或证据。

## 重复报告与重复 PoC

- `Go2_RCE_完全链路` 是 G2-001、G2-002 与 GO2-CHAIN-001 的组合副本，不作为独立漏洞迁移。
- `Go2_免token远程控制EXP` 与 G2-001 重复，PoC 按内容哈希去重。
- GO2 全链目录与两个拆分目录存在至少 27 组内容哈希重复文件；权威副本只归入单漏洞目录。
- R1 的 `AWS_root凭证泄露`、`云端API凭证泄露_DashScope博查` 与 `硬编码凭证全线泄露` 合并为 R1-009。
- GO2/R1 两份 btgatt-server 分片重组报告合并为 UT-001。
- GO2 GCM 解密 DoS 与 R1 `btgatt第二溢出_decrypt栈` 合并为 UT-002，但保留型号验证差异。
- R1 未闭环包中已标注与动态闭环包重复的 arm、chat_go、audio_detect、btgatt 和 OTA 报告不再重复编号。

## 禁止与外部保管材料

- GO2/R1 漏洞源目录共有 381 个文件、约 11.65 MiB；其中 3 个 ZIP、1 个 UPK 片段、2 个密钥 BIN、2 个 ELF、2 个 PYC 和 1 个 DOCX 不直接提交，共 11 项。
- 历史固件目录共有 23,585 个文件、约 11.61 GiB；其中 25 个文件超过 50 MiB，2,076 个文件属于固件、压缩包、APK、模型、共享库、抓包或同类禁止扩展名。这些材料只记录清单、大小、SHA-256 和本地路径。
- 完整解包固件树、完整日志树、APK、ONNX 模型、UPK/TAR/ZIP 固件以及未脱敏抓包一律不复制。
- 计划迁移的文本报告、Python/Shell/HTML 探针与小型日志均先在仓库外脱敏；任何二次扫描命中会转为外部材料清单而非提交。

## 计数

- GO2 漏洞：28（动态确认 5、静态确认 18、候选 3、已证伪 2）。
- R1 漏洞：43（动态确认 12、静态确认 25、候选 4、已证伪 2）。
- 跨型号漏洞：2（均至少在一个型号动态确认）。
- 合计稳定漏洞：73。
- 攻击链：5（4 条已动态闭环，1 条待动态闭环）。

## 本次实际盘点补充

- GO2 源目录实际为 `C:\zyh\work\漏洞整理\unitree_GO2`，214 个文件，约 8.24 MiB；R1 源目录实际为 `C:\zyh\work\漏洞整理\unitree_R1`，167 个文件，约 3.41 MiB。两者均只读。
- 历史固件目录实际为 `C:\zyh\work\宇树固件`，23,585 个文件，约 11.61 GiB；完整固件树和大型材料不进入 Git。
- 目标目录盘点时已有 251 个文件、约 283 KiB，无 `.git`；本次不覆盖既有内容。
- Claude 清单共 48 条：14 条主 JSONL 实际存在，34 条仅有历史索引或 memory；4 条核心会话进入精选，其余完整会话进入归档。

## 其他机器人扩展（2026-09-16）

新增统一入口：[其他机器人](其他机器人/README.md)。本次按“一个机器人/平台一个目录”整理，未改变 GO2、R1 或跨型号目录。

| 机器人/平台 | 候选主题 | 合并后权威报告 | 编号前缀 | 导入脚本 | 未提交材料 |
|---|---:|---:|---|---:|---:|
| 伽利略 | 21 | 21 | `GAL-` | 24 | 46 |
| 优必选人型 | 27 | 26 | `UBH-` | 29 | 29 |
| 优必选 AI 悟空 EDU | 4 | 4 | `UBW-` | 30 | 34 |
| 合计 | 52 | 51 | — | 83 | 109 |

处理规则：候选主题先按报告和 PoC 比对去重；脚本只保留小型文本材料并完成脱敏；完整依赖树、压缩包、私钥/证书、凭据、视频、截图、日志和超过 50 MiB 的证据只保留 SHA-256 与本地路径清单。逐主题明细见 [其他机器人迁移清单](其他机器人迁移清单.md)。
